# Copy this file to .env and fill in your own key.
# Required only if running scripts/data_generation/cv_arabizer_v1.py
# (the legacy Groq-based version — superseded by the local Ollama versions,
# v2 and v3, which need no API key at all).

GROQ_API_KEY=your-groq-api-key-here
